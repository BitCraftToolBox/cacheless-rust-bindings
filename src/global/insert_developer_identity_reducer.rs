// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct InsertDeveloperIdentityArgs {
    pub identity: String,
    pub developer_name: String,
    pub service_name: String,
    pub email: String,
    pub is_external: bool,
}

impl From<InsertDeveloperIdentityArgs> for super::Reducer {
    fn from(args: InsertDeveloperIdentityArgs) -> Self {
        Self::InsertDeveloperIdentity {
            identity: args.identity,
            developer_name: args.developer_name,
            service_name: args.service_name,
            email: args.email,
            is_external: args.is_external,
        }
    }
}

impl __sdk::InModule for InsertDeveloperIdentityArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `insert_developer_identity`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait insert_developer_identity {
    /// Request that the remote module invoke the reducer `insert_developer_identity` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`insert_developer_identity:insert_developer_identity_then`] to run a callback after the reducer completes.
    fn insert_developer_identity(
        &self,
        identity: String,
        developer_name: String,
        service_name: String,
        email: String,
        is_external: bool,
    ) -> __sdk::Result<()> {
        self.insert_developer_identity_then(
            identity,
            developer_name,
            service_name,
            email,
            is_external,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `insert_developer_identity` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn insert_developer_identity_then(
        &self,
        identity: String,
        developer_name: String,
        service_name: String,
        email: String,
        is_external: bool,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl insert_developer_identity for super::RemoteReducers {
    fn insert_developer_identity_then(
        &self,
        identity: String,
        developer_name: String,
        service_name: String,
        email: String,
        is_external: bool,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            InsertDeveloperIdentityArgs {
                identity,
                developer_name,
                service_name,
                email,
                is_external,
            },
            callback,
        )
    }
}
