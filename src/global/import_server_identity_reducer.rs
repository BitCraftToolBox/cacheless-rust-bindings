// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::server_identity_type::ServerIdentity;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct ImportServerIdentityArgs {
    pub records: Vec<ServerIdentity>,
}

impl From<ImportServerIdentityArgs> for super::Reducer {
    fn from(args: ImportServerIdentityArgs) -> Self {
        Self::ImportServerIdentity {
            records: args.records,
        }
    }
}

impl __sdk::InModule for ImportServerIdentityArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `import_server_identity`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait import_server_identity {
    /// Request that the remote module invoke the reducer `import_server_identity` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`import_server_identity:import_server_identity_then`] to run a callback after the reducer completes.
    fn import_server_identity(&self, records: Vec<ServerIdentity>) -> __sdk::Result<()> {
        self.import_server_identity_then(records, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `import_server_identity` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn import_server_identity_then(
        &self,
        records: Vec<ServerIdentity>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl import_server_identity for super::RemoteReducers {
    fn import_server_identity_then(
        &self,
        records: Vec<ServerIdentity>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp
            .invoke_reducer_with_callback(ImportServerIdentityArgs { records }, callback)
    }
}
