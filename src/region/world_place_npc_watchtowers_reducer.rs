// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::offset_coordinates_small_message_type::OffsetCoordinatesSmallMessage;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct WorldPlaceNpcWatchtowersArgs {
    pub watchtower_positions: Vec<OffsetCoordinatesSmallMessage>,
    pub watchtower_chunk_indexes: Vec<Vec<u64>>,
    pub energy: i32,
    pub upkeep: i32,
    pub building_desc_id: i32,
    pub biomes: Vec<i32>,
    pub dry_run: bool,
    pub log_results: bool,
    pub clear_and_level_ground: bool,
    pub ignore_biomes: bool,
    pub ignore_claims: bool,
    pub ignore_dimension_rules: bool,
}

impl From<WorldPlaceNpcWatchtowersArgs> for super::Reducer {
    fn from(args: WorldPlaceNpcWatchtowersArgs) -> Self {
        Self::WorldPlaceNpcWatchtowers {
            watchtower_positions: args.watchtower_positions,
            watchtower_chunk_indexes: args.watchtower_chunk_indexes,
            energy: args.energy,
            upkeep: args.upkeep,
            building_desc_id: args.building_desc_id,
            biomes: args.biomes,
            dry_run: args.dry_run,
            log_results: args.log_results,
            clear_and_level_ground: args.clear_and_level_ground,
            ignore_biomes: args.ignore_biomes,
            ignore_claims: args.ignore_claims,
            ignore_dimension_rules: args.ignore_dimension_rules,
        }
    }
}

impl __sdk::InModule for WorldPlaceNpcWatchtowersArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `world_place_npc_watchtowers`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait world_place_npc_watchtowers {
    /// Request that the remote module invoke the reducer `world_place_npc_watchtowers` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`world_place_npc_watchtowers:world_place_npc_watchtowers_then`] to run a callback after the reducer completes.
    fn world_place_npc_watchtowers(
        &self,
        watchtower_positions: Vec<OffsetCoordinatesSmallMessage>,
        watchtower_chunk_indexes: Vec<Vec<u64>>,
        energy: i32,
        upkeep: i32,
        building_desc_id: i32,
        biomes: Vec<i32>,
        dry_run: bool,
        log_results: bool,
        clear_and_level_ground: bool,
        ignore_biomes: bool,
        ignore_claims: bool,
        ignore_dimension_rules: bool,
    ) -> __sdk::Result<()> {
        self.world_place_npc_watchtowers_then(
            watchtower_positions,
            watchtower_chunk_indexes,
            energy,
            upkeep,
            building_desc_id,
            biomes,
            dry_run,
            log_results,
            clear_and_level_ground,
            ignore_biomes,
            ignore_claims,
            ignore_dimension_rules,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `world_place_npc_watchtowers` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn world_place_npc_watchtowers_then(
        &self,
        watchtower_positions: Vec<OffsetCoordinatesSmallMessage>,
        watchtower_chunk_indexes: Vec<Vec<u64>>,
        energy: i32,
        upkeep: i32,
        building_desc_id: i32,
        biomes: Vec<i32>,
        dry_run: bool,
        log_results: bool,
        clear_and_level_ground: bool,
        ignore_biomes: bool,
        ignore_claims: bool,
        ignore_dimension_rules: bool,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl world_place_npc_watchtowers for super::RemoteReducers {
    fn world_place_npc_watchtowers_then(
        &self,
        watchtower_positions: Vec<OffsetCoordinatesSmallMessage>,
        watchtower_chunk_indexes: Vec<Vec<u64>>,
        energy: i32,
        upkeep: i32,
        building_desc_id: i32,
        biomes: Vec<i32>,
        dry_run: bool,
        log_results: bool,
        clear_and_level_ground: bool,
        ignore_biomes: bool,
        ignore_claims: bool,
        ignore_dimension_rules: bool,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            WorldPlaceNpcWatchtowersArgs {
                watchtower_positions,
                watchtower_chunk_indexes,
                energy,
                upkeep,
                building_desc_id,
                biomes,
                dry_run,
                log_results,
                clear_and_level_ground,
                ignore_biomes,
                ignore_claims,
                ignore_dimension_rules,
            },
            callback,
        )
    }
}
