// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{
	self as __sdk,
	__lib,
	__sats,
	__ws,
};


#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct ProspectArgs {
    pub prospecting_id: i32,
    pub timestamp: u64,
}

impl From<ProspectArgs> for super::Reducer {
    fn from(args: ProspectArgs) -> Self {
        Self::Prospect {
            prospecting_id: args.prospecting_id,
            timestamp: args.timestamp,
}
}
}

impl __sdk::InModule for ProspectArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `prospect`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait prospect {
    /// Request that the remote module invoke the reducer `prospect` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`prospect:prospect_then`] to run a callback after the reducer completes.
    fn prospect(&self, prospecting_id: i32,
timestamp: u64,
) -> __sdk::Result<()> {
        self.prospect_then(prospecting_id, timestamp,  |_, _| {})
    }

    /// Request that the remote module invoke the reducer `prospect` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn prospect_then(
        &self,
        prospecting_id: i32,
timestamp: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl prospect for super::RemoteReducers {
    fn prospect_then(
        &self,
        prospecting_id: i32,
timestamp: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(ProspectArgs { prospecting_id, timestamp,  }, callback)
    }
}

