// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{
	self as __sdk,
	__lib,
	__sats,
	__ws,
};

use super::enemy_move_request_type::EnemyMoveRequest;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct EnemyMoveBatchArgs {
    pub requests: Vec::<EnemyMoveRequest>,
}

impl From<EnemyMoveBatchArgs> for super::Reducer {
    fn from(args: EnemyMoveBatchArgs) -> Self {
        Self::EnemyMoveBatch {
            requests: args.requests,
}
}
}

impl __sdk::InModule for EnemyMoveBatchArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `enemy_move_batch`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait enemy_move_batch {
    /// Request that the remote module invoke the reducer `enemy_move_batch` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`enemy_move_batch:enemy_move_batch_then`] to run a callback after the reducer completes.
    fn enemy_move_batch(&self, requests: Vec::<EnemyMoveRequest>,
) -> __sdk::Result<()> {
        self.enemy_move_batch_then(requests,  |_, _| {})
    }

    /// Request that the remote module invoke the reducer `enemy_move_batch` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn enemy_move_batch_then(
        &self,
        requests: Vec::<EnemyMoveRequest>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl enemy_move_batch for super::RemoteReducers {
    fn enemy_move_batch_then(
        &self,
        requests: Vec::<EnemyMoveRequest>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(EnemyMoveBatchArgs { requests,  }, callback)
    }
}

