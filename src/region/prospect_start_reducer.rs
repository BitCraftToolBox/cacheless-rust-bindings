// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{
	self as __sdk,
	__lib,
	__sats,
	__ws,
};


#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct ProspectStartArgs {
    pub prospecting_id: i32,
    pub timestamp: u64,
}

impl From<ProspectStartArgs> for super::Reducer {
    fn from(args: ProspectStartArgs) -> Self {
        Self::ProspectStart {
            prospecting_id: args.prospecting_id,
            timestamp: args.timestamp,
}
}
}

impl __sdk::InModule for ProspectStartArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `prospect_start`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait prospect_start {
    /// Request that the remote module invoke the reducer `prospect_start` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`prospect_start:prospect_start_then`] to run a callback after the reducer completes.
    fn prospect_start(&self, prospecting_id: i32,
timestamp: u64,
) -> __sdk::Result<()> {
        self.prospect_start_then(prospecting_id, timestamp,  |_, _| {})
    }

    /// Request that the remote module invoke the reducer `prospect_start` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn prospect_start_then(
        &self,
        prospecting_id: i32,
timestamp: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl prospect_start for super::RemoteReducers {
    fn prospect_start_then(
        &self,
        prospecting_id: i32,
timestamp: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(ProspectStartArgs { prospecting_id, timestamp,  }, callback)
    }
}

