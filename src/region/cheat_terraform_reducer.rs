// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{
	self as __sdk,
	__lib,
	__sats,
	__ws,
};


#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct CheatTerraformArgs {
    pub x: i32,
    pub z: i32,
    pub dimension: u32,
    pub delta: i32,
}

impl From<CheatTerraformArgs> for super::Reducer {
    fn from(args: CheatTerraformArgs) -> Self {
        Self::CheatTerraform {
            x: args.x,
            z: args.z,
            dimension: args.dimension,
            delta: args.delta,
}
}
}

impl __sdk::InModule for CheatTerraformArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `cheat_terraform`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait cheat_terraform {
    /// Request that the remote module invoke the reducer `cheat_terraform` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`cheat_terraform:cheat_terraform_then`] to run a callback after the reducer completes.
    fn cheat_terraform(&self, x: i32,
z: i32,
dimension: u32,
delta: i32,
) -> __sdk::Result<()> {
        self.cheat_terraform_then(x, z, dimension, delta,  |_, _| {})
    }

    /// Request that the remote module invoke the reducer `cheat_terraform` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn cheat_terraform_then(
        &self,
        x: i32,
z: i32,
dimension: u32,
delta: i32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl cheat_terraform for super::RemoteReducers {
    fn cheat_terraform_then(
        &self,
        x: i32,
z: i32,
dimension: u32,
delta: i32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(CheatTerraformArgs { x, z, dimension, delta,  }, callback)
    }
}

